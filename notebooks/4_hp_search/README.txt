Folder Description: 

    In this notebook we perform a Hyper-parameters search over the following models: 

    Deep Neural Network 
    Convolutional Neural Network 
    U-NET

    For the Deep Neural Network we have tried several deeps with 3,5,7,10 hidden layers (with regularizers). 

    For the Convolutional Neural Network we have not perform any search yed (a initial one was done one year ago with only 30k samples). 

    For the U-NET, we have previously obtained good hyper-parameters, the only search is perform over regularizers. 

